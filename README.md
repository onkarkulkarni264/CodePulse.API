# CodePulse is A Code blog App

## Local configuration
appsettings.json now contains placeholder values for API keys and credentials. Do not store real secrets in source control.
Use `appsettings.Development.json`, environment variables, or the ASP.NET Core Secret Manager for sensitive values in local development.
